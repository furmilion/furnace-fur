#pragma once

#ifndef _YM2609_MACROS_H
#define _YM2609_MACROS_H

#ifndef my_min
    #define my_min(a, b) ((a) < (b) ? (a) : (b))
#endif

#ifndef my_max
    #define my_max(a, b) ((a) > (b) ? (a) : (b))
#endif

#endif